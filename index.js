// add solution here
function theBeatlesPlay(player,instrument) {
  var allPlayers=[];
  for(var i=0;i<players.length;i++){
  allPlayers.push(players[i] + "plays" + instrument[i])
  }
  return allPlayers;
}
